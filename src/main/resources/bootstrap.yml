spring:
  application:
    name: auth-service
  cloud:
    config:
      uri: http://${CONFIG_SERVER_PROKARMA_EI_PIVOTAL_URL}
      fail-fast: true
      password: ${CONFIG_SERVER_PROKARMA_EI_PIVOTAL_PASSWORD}
      username: ${CONFIG_SERVER_PROKARMA_EI_PIVOTAL_USERNAME}