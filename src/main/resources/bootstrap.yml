spring:
  application:
    name: auth-service
  cloud:
    config:
      #uri: http://pkmst-config.cfapps.io
      uri: http://localhost:8888
      fail-fast: true
      password: user
      username: user
